[package]
name = "meeting-notes-summary"
version = "0.1.0"
edition = "2024"

[dependencies]
async-trait = "0.1.91"
meeting-notes-core = { version = "0.1.0", path = "../meeting-notes-core" }
reqwest = { version = "0.13.4", features = ["json"] }
serde_json = "1.0.151"

[dev-dependencies]
tokio = { version = "1.53.1", features = ["full"] }
